# Fabric Integration Plan for AIPrivateSearch

## Overview

This document adapts the Fabric install guide (`aips-fabric-install.md`) to integrate with AIPrivateSearch's existing architecture. Fabric provides remote prompt enhancement using Claude Haiku before queries are processed locally by Ollama.

## Architecture (Adapted for AIPS)

```
User selects collection → types query → clicks "Enhance" (optional)
        ↓
Express server → FabricService → Fabric server (enhance_[collection] pattern / Haiku)
        ↓
Enhanced prompt returned to UI → user reviews
        ↓
User clicks Submit → existing SearchOrchestrator → Ollama (local) → answer
```

**Key principle**: Fabric handles prompt enhancement only. All document processing, search, and answers remain local via Ollama. No document content leaves the user's machine.

---

## What Already Exists in AIPS

| Component | Current State | Fabric Role |
|-----------|--------------|-------------|
| `OllamaService.mjs` | Handles all AI generation | Unchanged — still does all answers |
| `meta-prompts.json` | Per-collection metadata (doc summaries, keywords, topics) | Becomes the source material for generating Fabric patterns |
| `addMetaPrompt` checkbox | Prepends collection context to queries | Fabric enhancement replaces/improves this |
| `system-prompts.json` | Assistant personality prompts | Unchanged — Fabric enhances the user query, not the system prompt |
| `user-prompts.json` | Pre-built queries per collection | Could be auto-generated by Fabric in future |
| `SearchOrchestrator.mjs` | Routes queries to 7 search methods | Receives the enhanced prompt, no changes needed |
| `CollectionsUtil.mjs` | Manages collection paths and names | Provides collection name → pattern name mapping |

---

## Integration Points (No New Apps Needed)

### Server-Side (fits into existing structure)

| New File | Location | Purpose |
|----------|----------|---------|
| `FabricService.mjs` | `server/s01_server-first-app/lib/services/` | Client for remote Fabric API (enhance prompt, check pattern exists) |
| `fabric.mjs` | `server/s01_server-first-app/routes/` | Express route: POST `/api/fabric/enhance` |
| `generatePattern.mjs` | `server/s01_server-first-app/scripts/` | Generates and uploads per-collection patterns to Fabric server |

### Client-Side

| Change | Location | Purpose |
|--------|----------|---------|
| "Enhance" button | `search.html`, `multi-mode-search.html` | Calls `/api/fabric/enhance` before submit |
| Enhanced prompt preview | Query field or modal | Shows user the improved prompt before submitting |

### Configuration

| File | Addition |
|------|----------|
| `.env-aips` | `FABRIC_URL=https://fabric.formr.net`, `FABRIC_API_KEY=your_secret_key` |
| `app.json` | `fabricEnabled: true/false` toggle |

---

## How It Maps to Existing AIPS Flow

### Current Flow (without Fabric)
```
User query → [optional: prepend meta-prompt] → SearchOrchestrator → Ollama → answer
```

### New Flow (with Fabric)
```
User query → [click Enhance] → FabricService → enhanced query returned to UI
           → User reviews/edits → Submit → SearchOrchestrator → Ollama → answer
```

The enhancement is **optional and user-controlled**. The existing `addMetaPrompt` checkbox can remain as a local fallback when Fabric is unavailable.

---

## Pattern Generation Strategy

AIPS already has rich collection metadata in `meta-prompts.json` (document types, word counts, topics, keywords, AI analysis). This is the perfect source for generating Fabric patterns.

### Pattern naming convention
```
enhance_[collection-name]
```
Examples: `enhance_USA-History`, `enhance_Family-Documents`, `enhance_Law-Office`

### When to generate/update patterns
- When a collection is created (from Collections page)
- When documents are added/removed from a collection
- When Document Index Cards are regenerated (since that updates metadata)
- When collection is re-embedded
- Manual trigger from Collections page ("Regenerate Pattern" button)

**Trigger points in existing code:**
- `routes/documents.mjs` → `POST /collections/create` — generate pattern after directory creation
- `routes/documents.mjs` → `POST /collections/:collection/upload` — regenerate pattern after file upload
- `routes/documents.mjs` → `DELETE /collections/:collection/files/:filename` — regenerate pattern after file removal
- `routes/multiSearch.mjs` → `POST /document-index-create` — regenerate pattern after index cards are built (best metadata available at this point)
- `routes/documents.mjs` → `POST /collections/:collection/index/:filename` — regenerate after embedding

The pattern generation should be fire-and-forget (non-blocking) so it doesn't slow down the user's action.

### Pattern content (built from existing meta-prompts.json)
The `generatePattern.mjs` script reads the collection's entry in `meta-prompts.json` and builds a **sanitized** Fabric system prompt. 

**What IS sent to Fabric (safe):**
- Collection name
- Document count
- Document types (legal, medical, HR, general)
- Topic categories
- Generic domain vocabulary and terminology

**What is NEVER sent to Fabric:**
- Filenames (may contain real names, case numbers, patient IDs)
- AI-generated summaries (may contain names, diagnoses, financial figures)
- Keywords extracted from content (may contain PII)
- DocIDs, timestamps, word counts
- Any specific names, numbers, or values from documents

### PII Risk by Collection

| Collection | Risk Level | Action |
|------------|-----------|--------|
| USA-History, My-Literature, A-Poem | None | Full topic/keyword context safe to send |
| Family-Documents | High | Domain context only (insurance, legal, financial) — no filenames or summaries |
| Medical-Practice | High | Domain context only (medical records, treatments) — no patient data |
| Law-Office | High | Domain context only (estate planning, contracts) — no client data |
| Human-Resources | High | Domain context only (HR, compliance, CDL) — no employee data |

### Sanitization approach
`generatePattern.mjs` will build patterns from a **hardcoded domain template** per collection type rather than from the actual metadata content. The template describes the domain vocabulary and query enhancement strategy without referencing any real document content.

Domain templates will be stored in `client/c01_client-first-app/config/safeDomainContext.json` — a dedicated config file mapping collection types to safe vocabulary and enhancement instructions.

### Pattern versioning
Patterns are named `enhance_[collection-name]` and updated in place. If breaking changes are needed, use `enhance_[collection-name]_v2` to avoid disrupting existing references.

---

## Ubuntu Server Setup (Same as fabric-install.md)

No changes needed to the server setup. Follow `aips-fabric-install.md`:
1. Install Go
2. Install Fabric
3. Get Anthropic API key
4. Generate Fabric secret key: `openssl rand -hex 32` — use this same key in the systemd service file on the server and in `.env-aips` on the Mac
5. Configure Fabric with `fabric --setup`
6. Configure DNS (GoDaddy A record for fabric.formr.net)
7. Start Fabric with systemd (`--serve --address :8081 --api-key`)
8. Configure Caddy reverse proxy (`/etc/caddy/sites/fabric-formr-net.caddy`)
9. Test with `curl`

> **Note**: PM2 does not work with Go binaries. Fabric runs as a systemd service.

---

## Cost Impact

| Action | Model | Cost |
|--------|-------|------|
| Enhance prompt (per click) | Claude Haiku 4.5 | ~$0.001 |
| All searches and answers | Ollama (local) | Free |
| Pattern generation (one-time per collection) | Claude Haiku 4.5 | ~$0.002 |

**Estimated monthly cost at 500 enhancements: ~$0.50**

---

## Security Considerations (AIPS-Specific)

- **API key storage**: `FABRIC_API_KEY` goes in `.env-aips` (already gitignored, already used for DB credentials)
- **No document content sent to Fabric**: Only the user's query text is sent for enhancement. Fabric patterns are built from sanitized domain templates — no filenames, summaries, or extracted keywords from actual documents are ever transmitted
- **No PII in patterns**: For sensitive collections (Medical, Law, HR, Family), patterns use generic domain vocabulary only. Real names, case numbers, patient data, and financial figures never leave the local machine
- **Fail-fast**: If Fabric is unreachable, the enhance button shows an error. Search still works normally with the raw query
- **Input validation**: Collection name validated against `CollectionsUtil.getCollectionNames()` before building pattern name (prevents injection)
- **Rate limiting**: Add to the `/api/fabric/enhance` route to prevent cost runaway (10 enhancements/minute per user is sufficient)
- **Query sanitization**: Validate raw query before sending to Fabric — enforce max length and strip control characters
- **No query logging**: Never log full user queries or enhanced prompts server-side in case they contain sensitive information
- **Pattern cache**: Cache pattern existence checks locally with a last-checked timestamp to avoid unnecessary round trips to the Fabric server
- **Fabric version pinning**: Document the installed Fabric version and test before upgrading — Fabric evolves quickly with new Claude model support

---

## Implementation Priority

### Phase 1: Core Integration
1. Create `FabricService.mjs` (enhance prompt, check pattern exists, SSE collection) with 8-10s timeout, 1-2 retries, and circuit breaker after repeated failures
2. Create `/api/fabric/enhance` route with rate limiting and query sanitization
3. Add "Enhance" button to search UI with "Enhancing with Claude Haiku..." spinner and cancel option
4. Add Fabric config to `.env-aips` and `app.json`
5. Graceful degradation: if Fabric is unreachable, auto-fall back to `addMetaPrompt` and show toast "Remote enhancement unavailable — using local context"
6. **Test**: 
   - Simple collection (USA-History) — confirm enhanced prompt returns
   - Sensitive collection (Medical or Law) — confirm no PII in pattern
   - Offline mode — confirm fallback works and search still completes
   - Malformed collection name — confirm validation rejects it

### Phase 2: Pattern Management
6. Create `generatePattern.mjs` using existing `meta-prompts.json` data
7. Add "Generate Pattern" button to Collections page
8. Auto-generate pattern when Document Index Cards are created
9. **Test**: Generate pattern for USA-History collection, confirm `enhance_USA-History` appears in `curl https://fabric.formr.net/patterns/names`, run Enhance with a USA-History query and verify domain-specific improvement

### Phase 3: Polish
10. Cache pattern existence checks with last-checked timestamp
11. Add enhance level option (Concise / Balanced / Thorough) mapping to different pattern variants
12. Show enhancement diff (before/after) so users see why it improved
13. Local fallback using Ollama if Fabric is down
14. Auto-enhance toggle per collection (with warning shown for sensitive collections)
15. **Test**: Disable Fabric server, confirm search still works with raw query and fallback toast is shown

---

## What NOT to Change

- `SearchOrchestrator.mjs` — receives the query (enhanced or not), no awareness of Fabric needed
- `OllamaService.mjs` — still handles all generation
- `ScoringService.mjs` — scores the answer regardless of whether prompt was enhanced
- Existing search methods — all 7 methods work the same way
- `system-prompts.json` — assistant personality is separate from query enhancement
- Document processing/embedding pipeline — completely unrelated to Fabric

---

## Dependencies

- **No new npm packages required** — uses native `fetch` (already available in Node 18+)
- **No changes to existing package.json**
- **Remote server required** — Ubuntu with Go, Fabric, systemd, Caddy (see fabric-install.md)
- **Anthropic API key required** — ~$5 minimum credit
- **Fabric URL**: `https://fabric.formr.net`

---

## Comparison: Current Meta-Prompt vs Fabric Enhancement

| Aspect | Current `addMetaPrompt` | Fabric Enhancement |
|--------|------------------------|-------------------|
| Intelligence | Static text prepended | AI-rewritten query with domain awareness |
| Cost | Free (local) | ~$0.001 per enhancement |
| Quality | Generic collection context | Tailored, specific, structured query |
| User control | On/off checkbox | Review and edit before submit |
| Availability | Always available | Requires remote server |
| Fallback | N/A | Falls back to addMetaPrompt or raw query |

Both can coexist. Fabric is the premium option; meta-prompt is the always-available local fallback.
